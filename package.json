{
  "name": "soundsnatcher",
  "version": "1.0.0",
  "description": "Paste a YouTube link, snatch the audio as MP3 or WAV — with optional BPM + key detection baked into the filename.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "doctor": "node scripts/doctor.js",
    "test": "node scripts/selftest.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "license": "MIT",
  "private": true
}
